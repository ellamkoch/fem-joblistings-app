//HeroBackground.
// This file shows the background image on every page, mobile or desktop.

import '@styles/_hero.scss';

export default function HeroBackground() {
    return(
        <div className="hero-container">
            <div className="hero-img"></div>
        </div>
    );
}
